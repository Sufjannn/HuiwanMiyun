请为我生成一个完整的移动端 H5/小程序风格的高保真 HTML 页面，产品名为“惠玩密云·青年玩法”，要求如下： 
 
【整体框架与 UI 风格】 
- **设备模拟**：手机容器尺寸 390×844，圆角 42px，采用柔和阴影，顶部保留深色刘海 (Notch) 及状态栏（时间、信号），模拟真实手机外观。 
- **色彩与质感**：整体背景采用年轻化、轻盈通透的自然渐变色（如 `#e0f2fe` → `#dcfce7` → `#f3e8ff`）。全面引入**玻璃态 (Glassmorphism)** 设计，所有卡片、导航栏、模态框需使用半透明背景并配合毛玻璃模糊效果 (`backdrop-filter: blur`)，辅以微弱的白色边框和弥散阴影。
- **页面与动画**：采用单页应用 (SPA) 架构。页面切换时必须包含平滑的渐显上浮动画 (`fadeIn` + `translateY`)。所有交互按钮需有明显的点击缩放反馈 (`transform: scale`)。
- **全局组件**：
  - 底部固定导航栏（玻璃态），五个标签：首页、接驳车、活动、论坛、地图。当前页导航图标需高亮（使用主色调渐变背景 `#10b981` 至 `#059669`），图标需有轻微上浮效果。 
  - 全局弹窗：深色毛玻璃材质，右上角有关闭按钮，2.5 秒后自动消失。
- **代码要求**：单文件结构（HTML+CSS+JS），无外部库依赖，完美适配移动端触摸操作。
 
【首页详细要求】 
1. **顶部品牌区**：Logo 用“密”字圆标（绿色渐变），标题“青年玩法”，副标题“惠玩密云·友好文旅体验”。 
2. **主题横向滑动卡片 (Theme Carousel)**： 
   - 弃用传统的按钮网格，改用占据主要视觉区域的横向滑动卡片。
   - 卡片需支持手指拖动，具有流畅的惯性滚动和自动居中吸附效果 (`scroll-snap-type: x mandatory`)。
   - 视觉层次：当前居中卡片放大至 1 倍且完全不透明，两侧非居中卡片缩小至 0.92 倍并降低透明度。滑动时利用 JS 实时计算中心点并平滑更新激活状态。
   - 卡片内容包含：主题专属徽章（如“💧 水主题”）、大字号景点名称、简短描述、以及倾斜放大的装饰 emoji。背景需使用该主题专属的渐变色。
   - 点击当前激活的卡片可跳转至该主题详情页。 
3. **集章进度模块**： 
   - 位于卡片下方，显示当前进度（如 2/4），进度条需有平滑的宽度过渡动画 (`transition: width 0.5s`)。
   - 展示四个主题徽章（水、林、日、云），已获得徽章应用亮色渐变，未获得显示灰态。
   - 提供一个大号的“模拟完成打卡任务”渐变按钮，点击后进度增加，徽章点亮，并弹出成功提示。
 
【主题详情页】 
- 由首页滑动卡片点击进入。顶部含返回按钮。 
- 内容包含：该主题的 Emoji 视觉大图（渐变背景）、详细图文介绍。
- 功能按钮：
  - **语音播报**：调用 Web Speech API 朗读当前主题介绍。
  - **📷 扫码集章**：点击后调用设备摄像头（需处理 HTTPS 环境和权限），在页面弹出取景框模态层。包含“拍照识别”按钮，点击后截取当前画面（可隐藏在 Canvas），随后模拟识别成功，发放对应主题徽章并更新进度。
 
【接驳车页】 
- 展示两条路线卡片（如密云城区→云龙涧/黑龙潭），明确标示单程价格和班次时间。
- 点击“立即预约”进入表单页：包含出发地/目的地（只读带入）、乘车人数、手机号、日期、时间下拉框。
- 行程类型提供单选（去程/回程/往返），总价需根据选项动态翻倍计算。提交后显示成功弹窗并延时返回。
 
【活动信息页】 
- 列表展示最新活动（如音乐会、摄影打卡）及特殊颜色标识的通知警告。
- 点击进入活动详情页，顶部有返回按钮，详情区域展示占位长文本。
 
【青年论坛页】 
- 帖子卡片展示：作者昵称、发布时间、文本内容、图片（如有）、点赞数和评论数。 
- **点赞功能**：每人每帖限点赞一次，点击后数字变化且图标变红，再次点击取消。 
- **详情与评论**：点击卡片进入详情，展示完整内容及评论列表区，底部固定输入框支持新增评论。
- **发帖功能**：右下角悬浮悬浮的绿色渐变 `+` 按钮，点击弹出表单。支持输入文字、上传本地图片（`FileReader` 实时预览）。发布后新帖插入列表顶部。
 
【景区地图页】 
- 使用横向滚动容器 (`overflow-x: auto`)，隐藏滚动条。 
- 内置真实地图图片（`map.png`），宽度自适应且允许左右拖拽查看。 
- 在地图指定坐标（按百分比定位）放置圆点标记。点击标记有缩放反馈，并触发包含该地点信息的全局弹窗。
 
【数据与状态持久化】 
- 核心状态（集章总数 `stampCount`、论坛帖子列表 `posts` 含点赞评论信息）必须使用 `localStorage` 进行存取，确保页面刷新或重开后数据不丢失。
- 需预置 2-3 条带有点赞和评论数据的初始帖子，提升初次加载体验。代码结构需清晰，合理划分各模块的数据绑定。
